# AGENTS.md

## 1. 목적

이 문서는 `flow-bi` 저장소에서 작업하는 사람과 AI Agent의 최상위 실행 규칙을 정의한다.

## 2. 지침 우선순위

지침이 충돌할 때는 다음 순서로 적용한다.

1. 시스템 Permission 및 안전 정책
2. `SECURITY.md`
3. 사람의 현재 요청과 명시적 승인
4. `docs/plans/active/`의 Active Plan
5. `ARCHITECTURE.md` 및 승인된 ADR
6. 관련 Product Spec / Design Doc
7. `docs/quality/quality-model.md`
8. `CONVENTIONS.md` 및 Frontend/Backend 영역 문서
9. 외부 참고 문서

하위 AGENTS.md는 루트 규칙을 구체화할 수 있지만 완화하거나 무효화할 수 없다.

중대한 충돌이나 모순을 발견하면 임의로 판단하지 않고 기록하고 사람의 확인을 요청한다.

## 3. 작업 전 확인

작업에 필요한 문서만 확인한다.

- 동일 실행에서 이미 확인했고 이후 변경되지 않은 문서는 전체를 반복해서 읽지 않는다. 필요한 경우 관련 구간만 다시 확인한다.
- 구조 또는 설계 변경 → ARCHITECTURE.md, 관련 ADR
- 보안·인증·권한 변경 → SECURITY.md
- 코드 작성 규칙 → CONVENTIONS.md
- 기능 요구사항 → 관련 Product Spec / Design Doc
- 제품 코드 구현·버그 수정 및 Harness 실행 → 관련 Active Plan
- 영역별 작업 → 해당 디렉터리의 AGENTS.md

## 4. 작업 범위

Agent는 다음 원칙을 반드시 준수한다.

- 필요한 최소 변경만 수행한다.
- 관련 없는 코드 정리, 대규모 리팩터링, 기술 교체를 함께 수행하지 않는다.
- 다른 작업자가 만든 변경을 되돌리거나 덮어쓰지 않는다.
- 생성 파일, 마이그레이션, 공개 API, DB 스키마 변경은 Plan에 명시된 경우에만 수행한다.
- 확정되지 않은 기술을 임의로 도입하지 않는다.
- 테스트 통과를 위해 요구사항이나 검증 규칙을 약화하지 않는다.
- Active Plan은 `ARCHITECTURE.md`나 승인된 ADR을 암묵적으로 변경할 수 없다. 변경이 필요하면 관련 문서가 선행되어야 한다.

범위 밖의 문제를 발견하면 수정하지 않고 기술 부채 또는 후속 작업으로 기록한다. 단, 즉각적인 보안 사고나 데이터 손실 위험은 작업을 중단하고 사람에게 알린다.

## 5. 문서 우선 개발

다음 변경은 구현 전에 관련 문서가 존재하거나 같은 Task에서 함께 갱신되어야 한다.

- 사용자에게 보이는 기능 또는 동작
- API 계약
- DB 구조 및 제약조건
- 인증·인가·개인정보 처리
- 모듈 경계 또는 주요 의존 방향
- 품질 게이트 및 완료 조건

현재 ERD와 backend/DB_SCHEMA.md는 초기 기준선으로 취급하며 승인 없이 구조 개선 목적으로 변경하지 않는다.

## 6. 구현 및 검증 원칙

- 코드 구현과 버그 수정은 승인된 예외가 없는 한 TDD를 따른다.
- TDD는 `Red → Green → Refactor` 순서와 각 단계의 검증 결과를 실행 기록에 남긴다.
- 버그 수정은 문제를 재현하는 실패 테스트를 먼저 작성하고 회귀 테스트로 유지한다.
- 테스트 삭제, 단언 약화 또는 검증 우회로 작업을 통과시켜서는 안 된다.
- 적용 가능한 테스트, 정적 분석, 빌드를 실행한다.
- 실행하지 못한 검증과 실패한 검증은 숨기지 않고 기록한다.
- 관련 API, DB 또는 사용자 동작이 변경되면 관련 문서를 함께 갱신한다.

## 7. 금지 행위

명시적 승인 없이 다음을 수행하지 않는다.

- 운영 환경 배포 또는 운영 데이터 변경
- 비밀정보 생성·노출·커밋
- 인증이나 권한 검사를 우회하는 코드 추가
- 데이터 삭제 또는 복구가 어려운 명령 수행
- DB 스키마의 파괴적 변경
- 의존성 또는 핵심 기술 스택 교체
- 요구사항 ID나 검증 실패 기록 삭제
- 리뷰를 통과한 것처럼 결과 조작

## 8. 사람의 승인이 필요한 변경

다음 항목은 명시적으로 요구하지않는한 구현하지고 넘어간 후 보고한다.

- 요구사항의 의미나 사용자 범위 변경
- 인증 방식, 권한 모델, 토큰 정책 변경
- 개인정보 보관 또는 노출 범위 변경
- 공개 API의 호환성을 깨는 변경
- DB 데이터 손실 가능성이 있는 마이그레이션
- 새로운 외부 서비스나 유료 API 도입
- AI가 사용자를 대신해 변경 작업을 실행하는 정책
- 품질 게이트의 완화 또는 검증 생략

## 9. 작업 보고

최종 보고에는 최소한 다음을 포함한다.

- 수행한 변경
- 충족한 요구사항과 인수 조건
- 실행한 검증 및 결과
- 실행하지 못한 검증과 이유
- 남은 위험 또는 기술 부채
- 사람의 결정이 필요한 사항
